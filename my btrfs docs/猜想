1.当给一个很大的文件做快照后，改变原始或者快照中的文件，并不是把整个文件复制了一遍，那内部到底是怎么实现的呢，cow的东西是什么呢？
2.小文件到多大的时候就不是小文件了？
3.文件名的长度限制，是define的吗？
4.目录项索引到多少个时，才会分配实际的块？
5.mkfs.btrfs时，创建了什么东西？创建了多少个节点？
6.compress选项是旧文件压缩还是新文件压缩？是新文件压缩，因为文件是要存储到硬盘的
